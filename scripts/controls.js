CONTROLS = getControls(SETTINGS);

function getControls(settings){
    return{
        _parent          : "control-panel",
        complexitySlider : null,

        reset : function(){
            // complexitySlider = createSlider();
            // complexitySlider.parent(this._parent);
        }
    }
    
}